name: E2E (Vercel preview)

on:
  deployment_status:

jobs:
  playwright:
    # Vercel fires deployment_status when a preview finishes. Only run on success
    # and skip production so this doesn't re-run against prod on every promote.
    if: >-
      github.event.deployment_status.state == 'success' &&
      github.event.deployment_status.environment != 'production' &&
      github.event.deployment_status.environment != 'Production'
    runs-on: ubuntu-latest
    # Keep this tag in sync with the @playwright/test version in package.json.
    # Mismatch → browsers baked into the image won't match Playwright's expected
    # revisions and tests will fail at startup.
    container:
      image: mcr.microsoft.com/playwright:v1.59.1-noble
      options: --user 1001
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Run Playwright against Vercel preview
        run: pnpm exec playwright test
        env:
          CI: "true"
          E2E_BASE_URL: ${{ github.event.deployment_status.target_url }}
          VERCEL_AUTOMATION_BYPASS_SECRET: ${{ secrets.VERCEL_AUTOMATION_BYPASS_SECRET }}

      - name: Upload Playwright traces
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-traces
          path: |
            test-results/
            playwright-report/
          retention-days: 7
          if-no-files-found: ignore
